@page
@model TravalApp.Models.ErrorViewModel

<h1>Error</h1>
<p>An error occurred while processing your request.</p>

@if (Model != null && !string.IsNullOrEmpty(Model.RequestId))
{
    <p>
        Request ID: <code>@Model.RequestId</code>
    </p>
}